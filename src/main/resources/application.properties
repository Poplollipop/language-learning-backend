spring.application.name=LanguageLearningBackend

spring.datasource.url=jdbc:postgresql://localhost:5432/${DB_NAME:language_learning}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ponytail: dev-only default secret, override with real JWT_SECRET env var in any shared/prod environment
jwt.secret=${JWT_SECRET:+95yafRnGjFJEVvOKDSmHBF7pmLTeNx/DqLfRNyfnZQ=}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# comma-separated list of allowed frontend origins
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:19006}
